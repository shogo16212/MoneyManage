<Window x:Class="MoneyManage.CategoryManageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoneyManage"
        mc:Ignorable="d"
                WindowStartupLocation="CenterScreen"
        Style="{StaticResource Default}"
        
        Title="CategoryManageWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Image Source="/images/icon.png" Height="50" Margin="5" HorizontalAlignment="Left"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Text="タイプ" Width="100" Margin="5"/>
            <ComboBox x:Name="entryComboBox" Width="200" Margin="5" DisplayMemberPath="EntoriesName"/>
            <TextBlock Width="100" Text="名前" Margin="5"/>
            <TextBox x:Name="nameTextBox" Width="200" Margin="5"/>
            <Button Margin="5" x:Name="addButton" Click="addButton_Click" Content="追加" Width="70"/>
        </StackPanel>

        <DataGrid x:Name="dataGrid" Grid.Row="2" Margin="5" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="タイプ" Binding="{Binding Entory.EntoriesName}" Width="*"/>
                <DataGridTextColumn Header="カテゴリー" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTemplateColumn Header="アクション" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="deleteButton" Click="deleteButton_Click" Content="削除" Width="70" Tag="{Binding}" Foreground="{StaticResource Primary}" Background="{StaticResource White}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button Width="100" x:Name="cancelButton" Click="cancelButton_Click" Content="キャンセル" Margin="5" Foreground="{StaticResource Primary}" Background="{StaticResource White}"/>
            <Button Width="100" x:Name="submitButton" Click="submitButton_Click" Content="登録" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
